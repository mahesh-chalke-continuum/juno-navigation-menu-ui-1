import React from "react";
import MenuItemLevel1 from "../../shared/components/menu-item-level-1";

const ConfigurationMenu = props => (
  <MenuItemLevel1 title="Configuration" {...props} />
);

export default ConfigurationMenu;
